p = 2;

a = p;

p = 3;

f = caml_alloc(a, p);

print_num(a);

Field(f,p) = 0;

f1 = Field(f, p);

print_num(f1);

// now trying linked list
zero = 0;
two_slots = 2;
slot1 = 1;
slot2 = 2;

head = caml_alloc(two_slots, zero);

Field(head, slot1) = 1;
Field(head, slot2) = caml_alloc(two_slots, zero);

head_val = Field(head, slot1);

print_num(head_val);

node1 = Field(head, slot2);

Field(node1, slot1) = 2;
Field(node1, slot2) = caml_alloc(two_slots, zero);

node2 = Field(node1, slot2);

Field(node2, slot1) = 3;
Field(node2, slot2) = 0;

node2_val = Field(node2, slot1);
print_num(node2_val);